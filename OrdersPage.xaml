<Page x:Class="Task4_2.OrdersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Task4_2"
      mc:Ignorable="d" 
      Title="OrdersPage">

    <ListView x:Name="listView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" SelectionChanged="listView_SelectionChanged">
        <ListView.View>
            <GridView>
                <GridViewColumn Width="50" Header="ID" DisplayMemberBinding="{Binding id}"/>
                <GridViewColumn Width="150" Header="ID покупателя" DisplayMemberBinding="{Binding buyer}"/>
                <GridViewColumn Width="100" Header="ID товара" DisplayMemberBinding="{Binding item}"/>
                <GridViewColumn Width="100" Header="Количество" DisplayMemberBinding="{Binding item_quantity}"/>
                <GridViewColumn Width="150" Header="Общая стоимость" DisplayMemberBinding="{Binding total_value}"/>
                <GridViewColumn Width="150" Header="Дата заказа" DisplayMemberBinding="{Binding date, StringFormat={}{0:hh:mm:ss dd.MM.yyyy}}"/>
            </GridView>
        </ListView.View>
    </ListView>
</Page>

